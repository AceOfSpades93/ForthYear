package search;

import java.util.ArrayList;
import java.util.List;

public class PuzzleSolver {
	private PuzzleBoard initial;
	private List<PuzzleBoard> sol = new ArrayList<>();
	
	public PuzzleSolver(PuzzleBoard board) {
		initial = board.clone();
		
		if (initial.isSolvable()) {
			solve();
		}
	}
	
	public boolean isSolvable() {
		return initial.isSolvable();
	}
	
	public Iterable<PuzzleBoard> solution() {
		return sol;
	}
	
	private void solve() {
		sol = new ArrayList<>();
		
		Priority
	}
	
	private static class Node {
		PuzzleBoard v;
		Node prev, next;
		
		public Node(PuzzleBoard v) {
			this.v = v;
			prev = next = null;
		}
		
		public Node(PuzzleBoard v, Node prev) {
			this(v);
			this.prev = prev;
		}
		
		public Node(PuzzleBoard v, Node prev, Node next) {
			this(v, prev);
			this.next = next;
		}
	};
}
